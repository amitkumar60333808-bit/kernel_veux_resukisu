name: Build ReSukiSU Kernel (Nebula KSU)
# Uses dev-sm6375 kernel source (Nebula) with KernelSU built-in
# Includes Image + dtbo.img for full device tree overlay support.

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/kernel-build.yml'

jobs:
  build-kernel:
    name: Build ReSukiSU Kernel (Nebula KSU)
    runs-on: ubuntu-22.04
    env:
      SKIP_SUSFS: "true"
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib git gnupg gperf imagemagick lib32ncurses5-dev lib32readline-dev lib32z1-dev liblz4-tool libncurses5-dev libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev unzip python3

      - name: Clone kernel source (Nebula dev-sm6375, ksu branch)
        run: |
          echo "=== Cloning dev-sm6375/kernel_xiaomi_sm6375 (ksu branch) ==="
          git clone --depth=1 --branch ksu https://github.com/dev-sm6375/kernel_xiaomi_sm6375.git kernel

      - name: Download AOSP clang-r487747c
        run: |
          echo "=== Downloading AOSP clang-r487747c ==="
          mkdir -p /tmp/toolchain
          curl -sL --max-time 570 "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/fd582b924a40/clang-r487747c.tar.gz" -o /tmp/clang.tar.gz
          ls -la /tmp/clang.tar.gz
          tar -xzf /tmp/clang.tar.gz -C /tmp/toolchain 2>&1 | tail -2 || true
          echo "=== Verify toolchain ==="
          /tmp/toolchain/bin/clang-17 --version | head -3
          ls /tmp/toolchain/bin/ | grep -cE "llvm-(ar|nm|objcopy|strip)$"
          echo "/tmp/toolchain/bin" >> $GITHUB_PATH
          echo "TOOLCHAIN_DIR=/tmp/toolchain" >> $GITHUB_ENV

      - name: Check for SUSFS support
        run: |
          cd kernel
          if [ "${SKIP_SUSFS:-false}" = "true" ]; then
            echo "=== SKIPPING SUSFS (isolation test) ==="
          else
            echo "=== Checking SUSFS fragments ==="
            ls arch/arm64/configs/vendor/susfs_fragment.config 2>/dev/null && echo "SUSFS fragment found" || echo "No SUSFS fragment"
          fi

      - name: Configure kernel
        run: |
          cd kernel
          echo "=== Using veux-qgki_defconfig (KSU built-in) ==="
          mkdir -p out
          make ARCH=arm64 LLVM=1 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- HOSTCC=gcc HOSTCXX=g++ O=out vendor/veux-qgki_defconfig
          echo "=== Config stats ==="
          echo "Configs: $(grep -c '=y' out/.config) =y, $(grep -c '=m' out/.config) =m"
          if [ "${SKIP_SUSFS:-false}" != "true" ]; then
            if [ -f arch/arm64/configs/vendor/susfs_fragment.config ]; then
              cat arch/arm64/configs/vendor/susfs_fragment.config >> out/.config
              echo "  Added SUSFS config fragment"
            fi
          else
            echo "  SKIP_SUSFS=true: NOT adding SUSFS configs"
          fi
          echo "CONFIG_LOCALVERSION=-ReSukiSU-veux" >> out/.config
          echo "=== Resolve deps ==="
          make ARCH=arm64 LLVM=1 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- HOSTCC=gcc HOSTCXX=g++ O=out olddefconfig
          echo "=== Verify critical configs ==="
          grep -E "^CONFIG_KSU=|^CONFIG_QCOM_SCM=|^CONFIG_DRM=|^CONFIG_SECURITY_SELINUX=|^CONFIG_BLK_DEV_DM=" out/.config || echo "WARN: some configs missing"

      - name: Build kernel + DTBs
        run: |
          cd kernel
          echo "=== Compiler ==="
          clang --version | head -3
          set +e
          make ARCH=arm64 LLVM=1 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- HOSTCC=gcc HOSTCXX=g++ O=out KCFLAGS="-w" -j$(nproc) 2>&1 | tee build.log
          BUILD_RC=${PIPESTATUS[0]}
          echo "Build exit code: $BUILD_RC"
          if [ $BUILD_RC -ne 0 ]; then
            echo "=== BUILD FAILED ==="
            grep -iE "error:" build.log | grep -v "warning:" | tail -20
            echo "=== LAST 30 LINES ==="
            tail -30 build.log
            exit $BUILD_RC
          fi
          echo "=== Build DTBs ==="
          make ARCH=arm64 LLVM=1 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- HOSTCC=gcc HOSTCXX=g++ O=out dtbs -j$(nproc) 2>&1 | tee -a build.log
          DTB_RC=${PIPESTATUS[0]}
          echo "DTB build exit code: $DTB_RC"
          exit $DTB_RC

      - name: Show build errors
        if: failure()
        run: |
          cd kernel
          grep -iE "error:" build.log | grep -v "warning:" | tail -20

      - name: Check output
        if: always()
        run: |
          echo "=== Kernel Image ==="
          ls -la kernel/out/arch/arm64/boot/Image* 2>/dev/null || echo "Image NOT found"
          echo "=== DTBO files ==="
          find kernel/out/arch/arm64/boot/dts -name "*.dtbo" 2>/dev/null | head -20
          echo "=== DTB files ==="
          find kernel/out/arch/arm64/boot/dts -name "*.dtb" 2>/dev/null | head -20
          echo "=== dtbo.img ==="
          ls -la kernel/out/arch/arm64/boot/dtbo.img 2>/dev/null || echo "dtbo.img NOT found"
          echo "=== dtb.img ==="
          ls -la kernel/out/arch/arm64/boot/dtb.img 2>/dev/null || echo "dtb.img NOT found"

      - name: Package outputs
        if: success()
        run: |
          cd kernel
          IMAGE=$(ls out/arch/arm64/boot/Image* 2>/dev/null | head -1)
          if [ -z "$IMAGE" ]; then
            echo "ERROR: No Image found"
            exit 1
          fi
          echo "Found kernel image: $IMAGE ($(wc -c < $IMAGE) bytes)"

          cd ..
          mkdir -p release

          cp kernel/$IMAGE release/Image

          if [ -f kernel/out/arch/arm64/boot/dtbo.img ]; then
            cp kernel/out/arch/arm64/boot/dtbo.img release/dtbo.img
            echo "dtbo.img included ($(wc -c < release/dtbo.img) bytes)"
          else
            echo "WARNING: dtbo.img not found, packaging individual dtbos"
            find kernel/out/arch/arm64/boot/dts -name "veux*.dtbo" -exec cp {} release/ \;
          fi

          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git
          cd AnyKernel3
          rm -f *.zip
          cp ../release/Image Image
          cp ../release/dtbo.img dtbo.img 2>/dev/null || cp ../release/*.dtbo . 2>/dev/null || true
          sed -i 's/kernel.string=.*/kernel.string=ReSukiSU-veux/' anykernel.sh
          sed -i 's/do.devicecheck=.*/do.devicecheck=1/' anykernel.sh
          sed -i 's/device.name1=.*/device.name1=veux/' anykernel.sh
          sed -i 's/device.name2=.*/device.name2=peux/' anykernel.sh
          zip -r9 ../ReSukiSU-veux.zip . -x ".git*"

          echo "=== Final zip contents ==="
          unzip -l ../ReSukiSU-veux.zip | grep -E "Image|dtbo|dtb"

      - name: Upload flashable zip
        if: hashFiles('ReSukiSU-veux.zip') != ''
        uses: actions/upload-artifact@v4
        with:
          name: ReSukiSU-kernel-veux
          path: ReSukiSU-veux.zip
          retention-days: 7

      - name: Upload dtbo.img separately
        if: hashFiles('kernel/out/arch/arm64/boot/dtbo.img') != ''
        uses: actions/upload-artifact@v4
        with:
          name: dtbo-img-veux
          path: kernel/out/arch/arm64/boot/dtbo.img
          retention-days: 7
